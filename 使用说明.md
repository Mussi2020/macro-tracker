# 宏观指标跟踪 · 自动更新使用说明

## 每天自动发生什么

Windows 任务计划程序中的 **`MacroTracker_DailyUpdate`** 任务每天 **09:00** 自动运行
`run_update.bat` → `update_macro.py`，依次完成：

1. **抓取 19 个指标**，分六个组：
   - 📉 利率：美债 10Y / 2Y
   - 🏛 美国宏观（月度）：CPI、PCE、PPI、非农就业、制造业景气指数
   - ⚖️ 估值（月度）：席勒 CAPE
   - 📊 风险资产：VIX、标普500、纳斯达克、HYG
   - 🛢 商品：布伦特、WTI、黄金、铜
   - 💵 美元：美元指数、美元兑人民币、美元兑日元

   数据源按 **Yahoo → 新浪财经 → FRED** 自动降级：Yahoo 在中国大陆会返回 403，
   脚本检测到后自动切到新浪财经与 FRED，无需人工干预。
   月度宏观指标 Yahoo 没有，直接走 FRED；CAPE 走耶鲁 Shiller 官方文件。
2. **计算前期摆动高点/低点**及与当前值的对比涨跌幅（详见下节）
3. 合并进 `宏观指标跟踪.xlsx` 的『历史数据』表（自动补缺、去重、排序，保留最近 12000 个交易日）
4. 刷新『最新看板』表（最新值、**同比**、近1月/近3月变化、前期高低点，涨红跌绿）
5. 重建『趋势图』表 —— 19 张 Excel 原生折线图，引用完整数据区域，数据更新后打开即最新
6. 重新生成 `宏观趋势图.html` 交互式仪表盘（可缩放、悬浮查看数值）
7. 写入前自动备份到 `backups/` 子目录（保留最近 10 份）

## 文件清单

| 文件 | 作用 |
|---|---|
| `宏观指标跟踪.xlsx` | 数据主文件（最新看板 / 历史数据 / 趋势图 三个工作表） |
| `宏观趋势图.html` | 交互式趋势仪表盘，双击用浏览器打开 |
| `update_macro.py` | 自动更新脚本（核心逻辑，可按需修改指标） |
| `run_update.bat` | 更新入口：自动探测 Python 后运行脚本（定时任务调用它） |
| `立即更新.bat` | 手动一键更新，双击即运行（内部调用 run_update.bat） |
| `.pylibs/` | Python 依赖（requests / openpyxl），随项目走，无需建虚拟环境 |
| `update.log` | 运行日志（最近 100 行） |
| `backups/` | Excel 自动备份 |

## 手动更新

双击 **`立即更新.bat`** 即可（不受定时限制，随时可刷）。

## 管理定时任务

- 查看任务：`schtasks /Query /TN MacroTracker_DailyUpdate /V`
- 手动触发一次：`schtasks /Run /TN MacroTracker_DailyUpdate`
- 暂停任务：`schtasks /Change /TN MacroTracker_DailyUpdate /DISABLE`
- 恢复任务：`schtasks /Change /TN MacroTracker_DailyUpdate /ENABLE`
- 删除任务：`schtasks /Delete /TN MacroTracker_DailyUpdate /F`

也可在「任务计划程序」图形界面（Win+R 输入 `taskschd.msc`）中找到该任务。

## 注意事项

- 更新时**请勿打开 Excel 文件**（文件被锁定会保存失败，脚本会自动重试 3 次，
  失败不影响原文件，下次运行会自动补上缺失日期）
- 若电脑在 09:00 关机：开机登录后任务会自动补跑一次（StartWhenAvailable 已启用）
- 修改运行时间：任务计划程序中编辑触发器，或删除后重建
- 数据源为 Yahoo Finance 免费接口，单指标偶发失败不影响其他指标更新
- 想增删指标：编辑 `update_macro.py` 顶部的 `INDICATORS` 列表
  （格式：列名, Yahoo代码, 显示名, 备注, 精度, 颜色，分组）

## 前期高点 / 低点怎么读

看板新增了 4 列：**前期高点 / 距高点 / 前期低点 / 距低点**（HTML 仪表盘每张卡片下方
以 `H`、`L` 两枚标签展示，但网页端取的是**所选范围内的实际最高/最低点**，口径不同，
见下一节）。

- **前期高点/低点**不是历史最高最低，而是**离今天最近的一次摆动转折**（ZigZag 算法，
  反向波动达到阈值才确认一次转折），所以永远是「上一波」的那个关键点位。
- **当前所处阶段**用**黄底加粗**标出，只跟这一格：
  - 最后一个转折是**低点** → 上行段，看「距低点」的涨幅（自上次低位涨了多少）
  - 最后一个转折是**高点** → 下行段，看「距高点」的跌幅（自上次高位跌了多少）
- 另一格仍保留参考：正值表示**已突破**该点位，负值表示**尚未回到**该点位。
- 日期格式为 `YY-MM-DD`，避免跨年误读。

举例（2026-09-10 数据）：

| 指标 | 现价 | 前期高点 | 距高点 | 前期低点 | 距低点 | 阶段 |
|---|---|---|---|---|---|---|
| 纳斯达克综合 | 26,253 | 26,683.9｜26-06-15 | -1.62% | **24,442.9｜26-07-29** | **+7.41%** | 上行，自 7/29 低点反弹 7.41% |
| VIX | 16.34 | **20.66｜26-07-29** | **-20.91%** | 15.03｜26-07-10 | +8.72% | 下行，自 7/29 高点回落 20.91% |

摆动阈值写在 `update_macro.py` 的 `SWING_PCT`（默认 5%），按指标波动率单独设过：
股指 5%~6%、汇率 2%~3%、利率与原油 10%、VIX 25%。觉得转折点太密或太稀，改这个
字典即可 —— 调大只认大级别转折，调小更贴近短期波动。
（该阈值**只影响 Excel 看板**；网页端已改为区间极值，不再走摆动算法。）

### HTML 仪表盘：高低点跟随所选范围动态变化

每张卡片的范围按钮（起始日 / 1周 / 1月 / 季度 / 半年 / 1年 / 3年 / 5年 / 10年 / 全部）
不只是缩放图表，**H、L 两枚标签会按当前选中范围重新计算**：

| 选中范围 | H / L 取什么 |
|---|---|
| 1周 / 1月 / 季度 / 半年 / 1年… | 该区间内的**实际最高点、最低点**（附发生日期） |
| 起始日 | 自评估起始日（默认近 12 个月）至今 |
| 全部 | 全序列极值（数据自 1980 年代起，百分比会非常大，仅供长周期参考） |

- `H` 后面＝当前值相对**区间最高点**的涨跌幅（≤0，越接近 0 越贴近区间顶）
- `L` 后面＝当前值相对**区间最低点**的涨跌幅（≥0）
- **黄底标的是更晚出现的那一个**：低点更晚＝刚从底部起来；高点更晚＝刚从顶部回落

每张图的选择会分别记住，下次打开自动恢复。

顶部**评估起始日**默认是**最近 12 个月**（滚动窗口，每天打开自动顺延，不用手动改）；
手动改过的日期会被记住，点「重置」回到近 12 个月。标「起始日」的按钮即跟随该日期。

> Excel 看板的高低点固定为**全序列**口径（不受上面这些选择影响），HTML 才随范围联动。

## 数据源与降级机制

脚本按 **Yahoo → 新浪财经 → FRED** 三级降级取数，全程自动：

| 源 | 覆盖指标 | 说明 |
|---|---|---|
| Yahoo（主） | 13 个日频指标 | 完整历史，最理想；一旦返回 403 就整体跳过 |
| 新浪财经 | Brent/WTI/Gold/Copper/DXY/USDCNY/USDJPY/VIX/SPX/NDX/HYG | 中国大陆可直连，取最近收盘 |
| FRED | 美债10Y/2Y、VIX、Brent/WTI 历史；以及 CPI/PCE/PPI/非农/制造业景气 5 个月度指标 | 官方数据；连续失败后自动熔断 3 天不再重试 |
| 耶鲁 Shiller | 席勒 CAPE | 官方 ie_data.xls，已停更于 2023-09 |

> **FRED 取数通道说明**：FRED 现统一用 Python `requests` 经沙箱代理抓取（实测比系统 curl 直连稳定——
> 旧 curl 子进程访问 FRED 会被对端重置 `rc=56`，且旧版 curl 不支持 `--retry-all-errors`）。
> 同时 `fred_cache.json` 做本地缓存：缓存 3 天内有效，日常运行直接读缓存、**不联网**；
> 仅缓存过期才联网，且实时失败会**降级使用（可能过期的）缓存**而非报错，保证每日运行不丢数据。
> 脚本内已移除旧版「FRED 失败熔断 3 天」逻辑——单序列失败不再拖垮全部，也不会再写入 `__fred_down__` 标记。

## 月度宏观指标与 CAPE

五个宏观指标 + CAPE 都是**月度**数据，和日频行情有三个不同：

1. **落点**：月度值记在该月**最后一个工作日**（避免被「剔除周末脏行」逻辑误删）。
2. **涨跌幅口径**：看板上的「近1月」对月度指标显示为**环比**（相对上一个数据点），
   「同比」列固定取 12 期前。日频指标则按自然日回看。
3. **发布滞后**：月度数据都要等官方发布（通常次月初到次月中），
   所以看板备注里标了**数据月份**，一眼能看出新鲜度。

| 指标 | 来源 | 起点 | 备注 |
|---|---|---|---|
| CPI | FRED `CPIAUCSL` | 1947 | 城市消费者口径，季调 |
| PCE | FRED `PCEPI` | 1959 | 美联储决策口径 |
| PPI | FRED `PPIACO` | 1913 | 全部商品 |
| 非农就业 | FRED `PAYEMS` | 1939 | 单位千人；月度新增写在备注列 |
| 制造业景气 | FRED `GACDISA066MSFRBNY` | 1967 | 纽约联储 Empire State；0 上方为扩张 |
| 席勒 CAPE | 耶鲁 ie_data.xls | 1881 | 官方停更于 2023-09，之后手工补 |

**关于 PMI**：ISM 制造业 PMI 没有免费的完整历史数据源（FRED 已下架该序列，
金十、TradingEconomics 的公开接口也都停用了），所以这里用**纽约联储 Empire State
制造业指数**替代——同样是月度制造业景气调查，0 上方代表扩张，能画完整历史曲线。
如果以后找到稳定的 ISM 数据源，改 `MACRO_FRED` 里的 ID 即可。

**关于 CAPE 停更**：官方文件最新只到 2023-09。之后的月份请手工补充到
**`cape_manual.csv`**（每行 `YYYY-MM,数值`，去掉行首 `#` 生效），下次更新自动合并。
文件里已经放好了格式说明和空行模板。

因此即使 Yahoo 临时不可用（如网络环境变化），看板也不会断更。
所有路径都跟随脚本自身位置，整个 `economy` 文件夹换盘/换电脑可直接搬走。
